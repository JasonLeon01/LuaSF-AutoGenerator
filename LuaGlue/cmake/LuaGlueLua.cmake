set(LUAGLUE_LUA_TARGET "" CACHE STRING "Existing Lua 5.5 CMake target")
if(NOT LUAGLUE_LUA_TARGET)
    set(_luaglue_saved_module_path "${CMAKE_MODULE_PATH}")
    list(PREPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}")
    find_package(Lua 5.5 EXACT REQUIRED MODULE)
    set(CMAKE_MODULE_PATH "${_luaglue_saved_module_path}")
    unset(_luaglue_saved_module_path)
    add_library(LuaGlueLua INTERFACE)
    target_include_directories(LuaGlueLua INTERFACE "${LUA_INCLUDE_DIR}")
    target_link_libraries(LuaGlueLua INTERFACE ${LUA_LIBRARIES})
    set(LUAGLUE_LUA_TARGET LuaGlueLua)
elseif(NOT TARGET "${LUAGLUE_LUA_TARGET}")
    message(FATAL_ERROR "LUAGLUE_LUA_TARGET must name an existing Lua 5.5 target")
endif()
